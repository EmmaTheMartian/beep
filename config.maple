dev_mode = true

http = {
	port = 8008
}

postgres = {
	host = 'localhost'
	port = 5432
	user = 'beep'
	password = 'beep'
	db = 'beep'
}

post = {
	title_max_len = 50
	body_max_len = 500
}

user = {
	username_min_len = 3
	username_max_len = 20
	username_pattern = '[a-z0-9_.]+'
	nickname_min_len = 1
	nickname_max_len = 20
	nickname_pattern = '\S.*'
	password_min_len = 12
	password_max_len = 72
	password_pattern = '.+'
}
